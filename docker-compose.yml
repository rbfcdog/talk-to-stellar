version: '3.9'

services:
  # Backend API (Node.js/Express + Integrated LangGraph Agent)
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: talktostellar-backend
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development
      - STELLAR_NETWORK=${STELLAR_NETWORK:-TESTNET}
      - STELLAR_HORIZON_URL=${STELLAR_HORIZON_URL:-https://horizon-testnet.stellar.org}
      - STELLAR_FRIENDBOT_URL=${STELLAR_FRIENDBOT_URL:-https://friendbot.stellar.org}
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
      - SUPABASE_JWT_SECRET=${SUPABASE_JWT_SECRET}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4}
      - TEMPERATURE=${TEMPERATURE:-0.5}
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - PORT=3001
      - JWT_SECRET=${JWT_SECRET:-dev-secret}
      - INTERNAL_API_SECRET=${INTERNAL_API_SECRET:-dev-secret}
      - AGENT_INGEST_SECRET=${AGENT_INGEST_SECRET:-dev-secret}
      - TELEGRAM_NOTIFY_URL=${TELEGRAM_NOTIFY_URL:-}
      - TELEGRAM_NOTIFY_SECRET=${TELEGRAM_NOTIFY_SECRET:-dev-secret}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN:-}
      - QUOTE_TTL_SECONDS=${QUOTE_TTL_SECONDS:-30}
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost:3000}
    volumes:
      - ./backend/src:/app/src
      - ./backend/tsconfig.json:/app/tsconfig.json
      - /app/node_modules
    networks:
      - talktostellar-network
    command: npm run dev
    restart: unless-stopped

  # Frontend (Next.js)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: talktostellar-frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_BACKEND_URL=${NEXT_PUBLIC_BACKEND_URL:-http://localhost:3001}
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://localhost:3001}
    volumes:
      - ./frontend/app:/app/app
      - ./frontend/components:/app/components
      - ./frontend/lib:/app/lib
      - ./frontend/styles:/app/styles
      - ./frontend/public:/app/public
      - /app/node_modules
    networks:
      - talktostellar-network
    command: npm run dev
    restart: unless-stopped
    # Don't wait for backend - they start in parallel

networks:
  talktostellar-network:
    driver: bridge

volumes:
  backend-logs:
    driver: local
