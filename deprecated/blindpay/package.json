{
  "name": "blindpay-pix-stellar",
  "version": "1.0.0",
  "description": "BlindPay PIX → Stellar Payin — complete frontend + API server",
  "license": "ISC",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "start:prod": "node --env-file=.env server.js --prod",
    "cli": "node --env-file=.env index.js",
    "cli:prod": "node --env-file=.env index.js --prod",
    "resume": "node --env-file=.env index.js --resume"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^15.0.0",
    "cors": "^2.8.6",
    "express": "^4.22.2",
    "qrcode": "^1.5.4"
  }
}
