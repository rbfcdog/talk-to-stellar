# Run 2026-06-13-1752

## What was implemented/changed this run

| File | Purpose |
|------|---------|
| `docs/insta-awards/deliverables/deliverable-1/PLAN.md` | Rewrote mandatory exploration findings, hook plan, migration decision, and remaining gaps. |
| `backend/migrations/20260611_00_transfers.sql` | Made transfers migration idempotent; added DB public ref sequence/function, indexes, and trigger. |
| `backend/migrations/20260611_01_transfer_events.sql` | Added append-only event guards and atomic lifecycle RPCs. |
| `backend/migrations/20260611_00_transfer_lifecycle_engine.down.sql` | Added rollback migration. |
| `backend/src/api/repository/transfer.repository.ts` | Added RPC-backed atomic create/transition methods, legacy lookup, and evidence lookup. |
| `backend/src/orchestration/decimal.ts` | Added decimal-string arithmetic helpers to avoid float math in lifecycle amounts. |
| `backend/src/orchestration/orchestrationLogger.ts` | Added strict JSON orchestration logs to stdout and `LOG_FILE`. |
| `backend/src/orchestration/TransferOrchestrator.ts` | Rebuilt lifecycle engine: strict transitions, idempotent evidence checks, reconciliation, and legacy sync bridge. |
| `backend/src/orchestration/stateMachine.ts` | Removed legal self-transition behavior; replay is now an audit event, not a transition. |
| `backend/src/orchestration/stellarWatcher.ts` | Replaced synthetic settlement with Horizon confirmation/failure watcher. |
| `backend/src/orchestration/types.ts` | Added `legacy_transfer_id` to create intent. |
| `backend/src/api/services/international-transfer.service.ts` | Mirrored existing Etherfuse/Stellar/payout stages into normalized transfers. |
| `backend/src/api/services/international-transfer.types.ts` | Added optional normalized orchestration refs for responses. |
| `backend/src/api/controllers/international-transfers.controller.ts` | Added token-auth normalized list/create/read behavior without breaking legacy routes. |
| `backend/src/api/routes/international-transfers.router.ts` | Added `GET /api/transfers` normalized list route. |
| `backend/src/api/controllers/ops.controller.ts` | Reworked `/ops` UI into a polished operational dashboard and API controller. |
| `backend/src/api/agent/tools.ts` | Added `public_ref` to chat-facing USD bank transfer intent output. |
| `backend/src/scripts/export-transfer-log.ts` | Fixed evidence path and included JSON log lines plus TransferEvents. |
| `backend/src/scripts/export-transfer-record.ts` | Added full transfer record evidence export. |
| `backend/scripts/export-transfer-log.ts` | Added reviewer-friendly script entrypoint. |
| `backend/scripts/export-transfer-record.ts` | Added reviewer-friendly script entrypoint. |
| `backend/package.json` | Added evidence export npm scripts. |
| `backend/tests/orchestration/orchestrator.test.ts` | Rebuilt lifecycle integration test for atomic transition repository contract. |
| `backend/tests/orchestration/stateMachine.test.ts` | Updated identity-transition expectation. |
| `docs/project-brain/*` | Updated architecture, data model, money flows, env, admin, integrations, overview, docs index, and grant status. |
| `docs/insta-awards/deliverables/deliverable-1/STATUS.md` | Corrected acceptance checklist and blockers. |
| `docs/insta-awards/deliverables/deliverable-1/MIGRATIONS.md` | Rewrote migration commands, rollback, expected schema, and run status. |

## Migrations to run

Recommended:

```bash
cd backend
supabase db push
```

Manual SQL order:

```text
1. backend/migrations/20260611_00_transfers.sql
2. backend/migrations/20260611_01_transfer_events.sql
```

Rollback:

```bash
cd backend
supabase db execute --file migrations/20260611_00_transfer_lifecycle_engine.down.sql
```

Run status: not run in this session. I did not have verified target Supabase migration context, so I did not modify the database.

## Commands executed and results

```bash
npm --prefix backend run build
```

Result: passed. TypeScript compiled successfully.

```bash
npm --prefix backend test -- --runInBand tests/orchestration/stateMachine.test.ts tests/orchestration/orchestrator.test.ts
```

Key output: `Test Suites: 2 passed, 2 total`; `Tests: 21 passed, 21 total`.

```bash
npm --prefix backend test -- --runInBand tests/international-transfer.routes.test.ts
```

Key output: `Test Suites: 1 passed, 1 total`; `Tests: 5 passed, 5 total`.

## Evidence produced this run

No final reviewer evidence was regenerated because migrations were not applied and no real Stellar testnet transfer was executed.

Updated evidence tooling:
- `backend/scripts/export-transfer-log.ts`
- `backend/scripts/export-transfer-record.ts`

## Open issues / next steps

1. Apply the D1 migrations to the target Supabase database.
2. Run the schema inspection SQL from `MIGRATIONS.md` and paste the output there.
3. Run one real Stellar testnet D1 transfer with `LOG_FILE` set.
4. Start the backend and capture `/ops` list/detail screenshots for that same `public_ref`.
5. Export `orchestration-logs-<public_ref>.json` and `transfer-record-<public_ref>.json` using the new scripts.
6. Manually check WhatsApp/Telegram response text includes `public_ref` for the USD bank transfer intent path.
