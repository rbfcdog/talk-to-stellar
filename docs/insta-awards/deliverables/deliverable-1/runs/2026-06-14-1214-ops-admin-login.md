# Run 2026-06-14-1214 - Ops Admin Login

## Scope

Replaced browser dashboard entry through query token with a DB-backed admin login for `/ops`.

## Files Changed

| File | Change |
|---|---|
| `backend/src/api/services/ops-admin-auth.service.ts` | Added `ops_admin_users` lookup, scrypt password verification, failure lockout, and active-admin checks. |
| `backend/src/api/controllers/ops.controller.ts` | Added `/ops/login` session creation, `/ops/logout`, HTTP-only cookie auth, CSRF checks, and DB verification on dashboard requests. |
| `backend/src/api/routes/ops.router.ts` | Added login/logout routes with sensitive rate limiting. |
| `backend/src/api/views/ops-dashboard.view.ts` | Added login page, logout form, operator identity chip, and login CSS using existing ops tokens. |
| `backend/migrations/20260614_00_ops_admin_auth.sql` | Added `ops_admin_users`, RLS, service-role-only policy, and `upsert_ops_admin_user(...)`. |
| `backend/migrations/20260613_00_full_schema.sql` | Added the ops admin auth schema to fresh bootstrap. |
| `backend/scripts/hash-ops-admin-password.ts` | Added password hash utility for admin bootstrap. |
| `backend/scripts/run-required-migrations.ts` | Applies required SQL files in order and can create or rotate the first ops admin after migrations. |
| `backend/tests/ops.routes.test.ts` | Added DB-backed login, CSRF, session-cookie, and redirect coverage. |
| `backend/.env.example` | Added ops admin login/session env vars. |
| `docs/project-brain/**` and D1/D3 docs | Updated auth, migration, and dashboard instructions. |

## Security Behavior

- Browser access to `/ops` and `/ops/transfers/:id` redirects to `/ops/login` without a valid session.
- Login checks `public.ops_admin_users.login` and `password_hash` via backend service-role DB access.
- Password hashes are generated by `backend/scripts/hash-ops-admin-password.ts`; plaintext passwords are not committed.
- Successful login sets `tts_ops_session` as HTTP-only, SameSite=Lax, Secure in production-like environments.
- Session JWT is signed with `JWT_SECRET` and contains an opaque CSRF token.
- Logout uses POST plus CSRF.
- JSON GET endpoints accept either the ops session cookie or legacy token auth.
- Unsafe JSON requests authenticated by cookie require `X-Ops-Csrf`; bearer/header/query token clients remain compatible.

## Admin Bootstrap

Note: `2026-06-14-1357-ops-admin-migration-supabase-fix.md` later moved the optional admin creation out of the SQL migration file so Supabase SQL Editor can run the migration directly. The migration runner command below still works.

```bash
cd backend
read -rs OPS_ADMIN_PASSWORD
export OPS_ADMIN_PASSWORD
export OPS_ADMIN_PASSWORD_HASH="$(npm run ops:hash-password --silent)"
export OPS_ADMIN_LOGIN="admin@example.com"
DATABASE_URL=postgresql://... npm run migrate:required
unset OPS_ADMIN_PASSWORD OPS_ADMIN_PASSWORD_HASH
```

Then start the backend and open:

```text
http://localhost:3001/ops/login
```

## Commands Run

```bash
npm --prefix backend run build
# PASS

npm --prefix backend test -- --runInBand tests/ops.routes.test.ts tests/ops-history.repository.test.ts tests/orchestration/stateMachine.test.ts tests/orchestration/orchestrator.test.ts tests/international-transfer.routes.test.ts
# PASS: 5 suites, 33 tests

OPS_ADMIN_PASSWORD='correct horse battery staple' npm --prefix backend run ops:hash-password --silent
# PASS: printed scrypt salt/hash

MIGRATION_DRY_RUN=1 OPS_ADMIN_LOGIN='Admin@TalkToStellar.test' OPS_ADMIN_PASSWORD_HASH='salt:hash' npm --prefix backend run migrate:required --silent
# PASS: listed 20260613 bootstrap and 20260614 ops-admin migration; admin bootstrap enabled for normalized login

git diff --check
# PASS
```

## Open Items

- Apply `backend/migrations/20260614_00_ops_admin_auth.sql` to the target Supabase database with a real generated `OPS_ADMIN_PASSWORD_HASH`.
- Capture updated `/ops/login` and post-login dashboard screenshots after the admin exists in the target DB.
