# TalkToStellar — Demo Video Script (1:00–1:30)

A tight, record-ready walkthrough, framed around the **WhatsApp** interface:
**chat → receive dollars → yield → cash out**. Target **~75–90 seconds**.
Read the narration in **bold**; the *italic* lines tell you what to show and click.

> Tip: record each screen as a short clip, then stitch. Have a logged-in test
> account ready (email `rodtretinha@gmail.com`) so balances are populated. The
> screens open from links the agent sends **inside WhatsApp** — show that hop at
> least once so it's clear the whole product lives in chat.
> Target ~200–220 spoken words = ~80s at a natural pace.

---

## 0:00 – 0:12 · The problem (WhatsApp chat)

*Screen: the **WhatsApp** conversation with TalkToStellar. A couple of messages
in Portuguese already on screen.*

> **"In Latin America, earning dollar yield still means crypto wallets, seed
> phrases and gas. We built TalkToStellar — a dollar bank you run entirely from
> WhatsApp. No app to install, no wallet, no jargon. Everything you'll see is
> live on Stellar mainnet."**

*Action: type "quero receber dólares dos EUA" and send. The agent replies with a
link — tap it to open the Receive screen.*

---

## 0:12 – 0:30 · Receive dollars — virtual account (on-ramp)

*Screen: the **Receive dollars** page. Point to "Received so far" and the
**Account holder** name, then the deposit-account card.*

> **"It opens a virtual US account — real routing and account number. I send a
> normal wire or ACH from any US bank, and dollars land here as digital dollars.
> That on/off-ramp is live on mainnet today through our regulated rail — I never
> touch the blockchain."**

*Action: scroll the deposit-account card (bank, beneficiary, account number,
routing). One-tap copy on a field to show it's real.*

---

## 0:30 – 0:52 · Yield — the dollars earn by default (`/rendimentos`)

*Screen: the **Returns** (rendimentos) page. Show the balance, the live gain
curve (gains only, per-minute), and the APY.*

> **"By default the money doesn't sit idle — it's put to work. We're live on
> mainnet across two protocols: DeFindex vaults and Blend lending. Here's my
> balance and the yield curve, updating every minute. Under **Advanced** there's
> more: extra Blend markets we're rolling out, and a Soroswap integration we're
> wiring in now."**

*Action: tap the **USDC yield** product → set a small amount → tap **Supply** and
show the green "✓" confirmation. Open **Advanced options** for ~1s to reveal the
other markets.*

---

## 0:52 – 1:10 · Cash out — send dollars back (off-ramp)

*Screen: the **Withdraw / cash out** page. Point at "Money available to withdraw"
(it includes what's in yield).*

> **"When I want it out, I cash back to my US bank over wire or ACH. The money's
> in yield, so the system automatically pulls exactly what I'm sending out of the
> vault first — same as it auto-invested it. I just pick the bank and the amount."**

*Action: select a saved destination bank → type an amount → tap confirm. Show the
sent/confirmation state.*

---

## 1:10 – 1:20 · Roadmap + close (back to WhatsApp)

*Screen: cut back to the WhatsApp chat.*

> **"Wire and ACH are live now; PIX for Brazil is in compliance and lands next.
> Real dollars in, real mainnet yield, real cash-out — all from a chat. That's
> TalkToStellar."**

---

## Screen checklist (in order)

| # | Screen | Route | Show |
|---|--------|-------|------|
| 1 | WhatsApp chat | (WhatsApp) | conversational entry; agent replies with a link |
| 2 | Receive dollars (on-ramp) | `/wire-onramp` | received total, account holder, virtual-account bank/routing/account, one-tap copy |
| 3 | Returns / yield | `/rendimentos` | balance + per-minute gain curve, DeFindex + Blend live, USDC supply → ✓, **Advanced** (more Blend + Soroswap) |
| 4 | Withdraw (off-ramp) | `/usd-withdraw` | "available" includes yield, destination bank, amount, confirm |
| 5 | WhatsApp chat | (WhatsApp) | roadmap (PIX coming) + closing line |

## Talking-points cheat sheet (keep it accurate)

- **Interface:** everything runs through **WhatsApp**; screens open from links the
  agent sends in chat.
- **On/off-ramp:** **live on mainnet** — receive via a **virtual account** (wire/ACH
  in), send dollars **out** (wire/ACH), all in **USDC**, via Bridge.
- **PIX (Brazil):** **not live yet** — in **compliance**, coming soon. Say "coming",
  don't demo it.
- **Yield:** **DeFindex** + **Blend** both **live on mainnet**. Money is auto-allocated
  and auto-redeemed when you spend.
- **Advanced:** more **Blend** markets being implemented; **Soroswap** integration
  just starting. Frame as "rolling out / wiring in", not finished.

## Recording notes

- Use **Live / Real** mode on the network toggle so balances look real (one
  toggle, top of the yield page).
- Portuguese on screen + English narration reads as "built for the market, told
  to the world" — that's fine.
- Don't show the access-password gate or any operator/detail panels — keep it
  customer-facing.
- If a live supply/withdraw is risky on camera, pre-fund and use a small amount
  (e.g. 1 USDC) so the ✓ confirmation is genuine.
- Show the **WhatsApp → link → screen** hop at least once so the "all from chat"
  claim is obvious.
